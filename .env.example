# Copy this file to .env (gitignored) and fill in real values, or export the
# variables directly in your shell. Never commit real tokens/passwords.
#
# See docs/DEPLOYMENT.md for how to obtain/rotate DEPLOY_TOKEN.

DEPLOY_TOKEN=
