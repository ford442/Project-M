{
  "name": "@projectm/html-host",
  "private": true,
  "description": "Shared browser host modules for projectM (typecheck only; not published)",
  "type": "module",
  "devDependencies": {
    "typescript": "5.8.3"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
