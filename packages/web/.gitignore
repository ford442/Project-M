# Build output — regenerated by `npm run build` / prepack (see scripts/build.mjs)
/dist/

# Intermediate vendored copy of the html/ closure; bundled into dist/.
/staging/

# WASM artifacts fetched for the local playground (npm run playground).
/playground/pm/

# esbuild bundle metadata (analysis only; see scripts/build.mjs).
/build-metafile.json
