diff --git a/flac-player/bundle.js b/flac-player/bundle.js
index 1234567..abcdef0 100644
--- a/flac-player/bundle.js
+++ b/flac-player/bundle.js
@@ -XXXX,YY +XXXX,YY @@  (around the projectm-audio bridge)
 // Existing legacy code (keep for compatibility during transition)
 if (!window.opener) return () => {};
 try {
   const t = new BroadcastChannel("projectm-audio");
   const n = new Float32Array(e.fftSize);
   let r;
   const a = () => {
     e.getFloatTimeDomainData(n);
-    t.postMessage({type:"pcm", buffer: n.slice()});
+    const buf = n.slice();
+    t.postMessage({type:"pcm", buffer: buf, channels: 1});
+
+    // NEW: Primary cross-origin safe path (works for popups and iframes)
+    try {
+      if (window.opener) {
+        window.opener.postMessage({type: "pcm", buffer: buf, channels: 1}, "*");
+      } else if (window.parent && window.parent !== window) {
+        window.parent.postMessage({type: "pcm", buffer: buf, channels: 1}, "*");
+      }
+    } catch (ex) {
+      console.debug("[projectMBridge] postMessage failed (non-fatal):", ex);
+    }
+
     r = requestAnimationFrame(a);
   };
   return r = requestAnimationFrame(a);
 } catch(e) {
   return console.debug("[projectMBridge] BroadcastChannel not available:", e), () => {};
 }

 // Recommended: also expose a clean function the host can call if it wants to
 // window.__projectM_sendPCM = (float32Array, channels=1) => { ... postMessage logic ... }
